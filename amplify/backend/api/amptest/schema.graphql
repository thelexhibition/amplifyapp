type SIGNALS @model @auth(rules: [{ allow: public }]) {
  id: ID!
  datetime: String
  name: String
  description: String
  assignee: String
  group: String
  workflow: Boolean
  chart_data: String
  createdAt: String
  updatedAt: String
}
